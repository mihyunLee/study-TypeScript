{
  "compilerOptions": {
    /* 타입스크립트를 자바스크립트로 변환하는 과정이나 타입 검사 등의 상세한 옵션 설정 */
    "target": "ES5" /* 컴파일 시 생성되는 자바스크립트의 버전 설정, ESNext: 자바스크립트 최신 버전 */,
    "module": "ESNext" /* 모듈 시스템 설정 */,
    "outDir": "dist" /* 컴파일 된 자바스크립트 파일의 위치 설정 */,
    "strict": true /* 타입스크립트의 컴파일 엄격도 설정 */,
    "moduleDetection": "force" /* 타입스크립트가 각각의 파일을 어떤 모듈로 인식할 것인지 결정한다. */
  },
  "ts-node": {
    "esm": true /* ts-node 패키지가 ESModule 시스템으로 동작할 수 있게 설정 */
  },
  "include": ["src"] /* 컴파일 할 파일의 범위를 지정할 수 있다. 
	현재는 src 폴더 아래에 있는 모든 파일을 컴파일 한다. */
}
